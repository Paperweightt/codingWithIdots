// to get the dimension type object its a process
import { world } from "@minecraft/server"

const overworld = world.getDimension('overworld')
const nether = world.getDimension('nether')
const theEnd = world.getDimension('the_end') // could be theEnd idk


//fillBlocks(begin: Vector3, end: Vector3, block: BlockPermutation | BlockType | string, options?: BlockFIllOptions)
//getBlock(location: Vector3): Block | undefined

